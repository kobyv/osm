---
name: python 
description: instructions for creating and updating Python code
---

- Target Python 3.12+
- Use Python typing
- Avoid imports outside of stdlib if makes sense. If not, use `uv`.
- Format code using `ruff format`
- Check code using `ruff check` and `mypy`
- Avoid `Any` type if makes sense
